export type Earthquake = {
    id: string
    magnitude: number
    place: string
    latitude: number
    longitude: number
    kmDepth: number
    earthquakeTime: string
    code: string
    url: string
}